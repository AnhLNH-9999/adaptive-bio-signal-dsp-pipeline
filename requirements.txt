# Core pipeline: adaptive filters, wavelet features, Q15 fixed-point, MACs analysis
numpy>=1.24,<3
scipy>=1.10
PyWavelets>=1.4
matplotlib>=3.7
Cython>=3.0            # optional speed-ups: python setup.py build_ext --inplace

# Real-dataset stages (weeks 3-4): BCI Competition IV-2a via MOABB, CSP via MNE, LDA via scikit-learn
mne>=1.5
moabb>=1.0
scikit-learn>=1.3

# Neural-network branch (week 7)
torch>=2.1
brevitas>=0.10         # quantisation-aware training (16-bit ~ Q15)

# Notebook + tests
jupyter>=1.0
nbformat>=5.9
pytest>=7.0
