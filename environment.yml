name: eeg-dsp-pipeline
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy>=1.24,<3
  - scipy>=1.10
  - pywavelets>=1.4
  - matplotlib>=3.7
  - cython>=3.0
  - scikit-learn>=1.3
  - jupyter
  - nbformat
  - pytest
  - pip
  - pip:
      - mne>=1.5
      - moabb>=1.0
      - torch>=2.1
      - brevitas>=0.10

# conda env create -f environment.yml && conda activate eeg-dsp-pipeline
